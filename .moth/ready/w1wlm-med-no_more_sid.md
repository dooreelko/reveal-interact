sid aka session id should be the token that is used to create a new session. this would allow to reconnect to an existing one.
for this task update IDEA.md as well
every function as a first step should verify whether the session/token is valid
